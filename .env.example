APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:y5PFleAAlEY/Z46vrNukMs0rwmhJkZrzzM8xUbBXXxQ=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432 # Coloquem a porta da vossa base de dados aqui (se for diferente da porta padrão que é esta que já está)
DB_DATABASE= # Coloquem a nome da vossa base de dados aqui
DB_USERNAME= # Coloquem o nome de utilizador da vossa base de dados aqui
DB_PASSWORD= # Coloquem a senha da vossa base de dados aqui

GEMINI_API_KEY="AIzaSyDulFNqakBghOOyhNKBjIHCaa2aIPdYkaw" # não mudem
GEMINI_MODEL=gemini-2.5-flash # podem escolher entre os seguintes modelos: "gemini-2.5-flash", "gemini-2.0-flash", "gemini-1.5-flash" e "gemini-1.0-flash"
GEMINI_FALLBACK_MODEL=gemini-2.0-flash # podem escolher entre os seguintes modelos: "gemini-2.5-flash", "gemini-2.0-flash", "gemini-1.5-flash" e "gemini-1.0-flash"
#Mas de preferencia nn mudem os modelos, apenas se quiserem testar.

GEMINI_TIMEOUT=60
GEMINI_MAX_RETRIES=4

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
